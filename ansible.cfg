# Generated by Anaconda 38.23.4
# Generated by pykickstart v3.47
#version=F38
# Use graphical install
graphical

# Keyboard layouts
keyboard --vckeymap=pl --xlayouts='pl'
# System language
lang pl_PL.UTF-8

url --mirrorlist=http://mirrors.fedoraproject.org/mirrorlist?repo=fedora-38&arch=x86_64
repo --name=update --mirrorlist=http://mirrors.fedoraproject.org/mirrorlist?repo=updates-released-f38&arch=x86_64

%packages
@^minimal-environment
moby-engine
%end

network --bootproto=dhcp --device=eth0 --onboot=yes --ipv6=auto
network --hostname=fedora2

# Run the Setup Agent on first boot
firstboot --enable

# Generated using Blivet version 3.7.1
ignoredisk --only-use=sda
autopart
# Partition clearing information
clearpart --none --initlabel

# System timezone
timezone Europe/Warsaw --utc

# Root password
rootpw --iscrypted $y$j9T$hFgMlLoCpyGS.g6ES.dyJzB8$N7yEoNCMHvknbPNrwMeRfDLLgyLZh                                                                                                             B7y2TfCDXz8kP.
user --groups=wheel --name=waldek --password=$y$j9T$LrEmfVR4cj1SNGd/DVeiSuin$dBO                                                                                                             wOWWlfjOEnuR30QX7fapqJRJpGgqfGvGVUBS.4uD --iscrypted --gecos="Waldek Świergosz"

%post --erroronfail

cat << 'EOF' > /etc/systemd/system/my-docker.service

usermod -aG docker marcel
systemctl enable docker

[Unit]
Description=Pobrano obraz 
Requires=docker.service
After=docker.service

[Service]
Type=oneshot
RemainAfterExit=yes
ExecStart=/usr/bin/docker pull vvaldekk/devops:1.1.7
ExecStart=/usr/bin/docker run -di -p 8081:8081 --name devops vvaldekk/devops:1.1.7

[Install]
WantedBy=multi-user.target
EOF

systemctl daemon-reload
systemctl enable my-docker.service
systemctl start my-docker.service

%end
